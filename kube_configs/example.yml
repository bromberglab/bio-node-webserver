# TODO: Copy to secret.yml and fill vars.

apiVersion: v1
kind: Secret
metadata:
  name: secrets-config
data:
  db_pw: "<goes here>"
  db_host: "<goes here>"
  sendgrid_key: "<goes here>"
  sendgrid_sender: "<goes here>"
